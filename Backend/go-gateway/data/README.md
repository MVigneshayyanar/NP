# Go Gateway Data Directory
This directory stores static data files used by the Go gateway (paint database, etc.).
Currently, the paint database is embedded in the Go code. This directory is reserved for future expansion.
